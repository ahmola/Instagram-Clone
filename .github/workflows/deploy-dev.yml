name: Build & Deploy for Dev (Local Action Runner)

on:
    push:
        branches: ["dev"]

jobs:
    call-template:
        uses: .github/workflow/build-template.yml@dev
        with:
            service: "config-server"
            port: 8888
